# Reproducible evidence

This directory contains machine-readable addresses, dated state snapshots, and read-only verification scripts supporting the canonical research documents.

Snapshots record observations, not permanently true values. Run `scripts/verify-state.sh` to refresh mutable contract state and `scripts/verify-control-path.sh` to reproduce the read-only MPC authorization simulation. Create a new reviewed snapshot rather than overwriting history.
